#include <stdio.h>

int main(){
    char first[30];
    char second[30];
    scanf("%s", &first);
    scanf("%s", &second);
    printf("You entered: %s and %s",first,second);
    return 0;

}